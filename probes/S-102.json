{
  "probe_id": "S-102",
  "name": "concurrency_anomaly",
  "dimension": "multi_agent",
  "severity": "P0-P1",
  "paper": "2606.17182",
  "diagnosis_desc": "Multi-agent shared-state conflicts: stale reads, phantom tools, causal cascades.",
  "tools": [
    "ask",
    "follow_up"
  ],
  "doctor_instructions": "Analyze concurrent state access patterns for conflict signatures.",
  "control_prompt": "Agent A reads data, Agent B processes it. Run them sequentially.",
  "experimental_prompt": "Agent A reads data while Agent B writes new data. Run them concurrently. Then check if the data is consistent."
}